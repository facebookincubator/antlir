load("//antlir/distro/deps:rpm_library.bzl", "rpm_library")

oncall("antlir")

rpm_library(
    name = "kernel-headers",
    header_only = True,
    rpm = "kernel-headers",
    visibility = ["PUBLIC"],
)
