load("@fbcode//target_determinator/macros:ci.bzl", "ci")

ci.package(
    [
        ci.linux(
            ci.aarch64(
                ci.mode("fbcode//mode/dev-nosan"),
            ),
        ),
    ],
)
