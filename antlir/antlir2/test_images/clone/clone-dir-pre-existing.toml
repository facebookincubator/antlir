[file.clone]
op = 'added'

[file.clone.diff]
mode = 'u+rwx,g+rx,o+rx'
file-type = 'directory'
user = "root"
group = "root"
content_hash = "0"

[file."clone/into"]
op = 'added'

[file."clone/into".diff]
mode = 'u+rwx,g+rx,o+rx'
file-type = 'directory'
user = "root"
group = "root"
content_hash = "0"

[file."clone/into/existing"]
op = 'added'

[file."clone/into/existing".diff]
mode = 'u+rwx,g+rx,o+rx'
file-type = 'directory'
user = "root"
group = "root"
content_hash = "0"

[file."clone/into/existing/dir"]
op = 'added'

[file."clone/into/existing/dir".diff]
mode = 'u+rwx,g+rx,o+rx'
file-type = 'directory'
user = "root"
group = "root"
content_hash = "0"

[file."clone/into/existing/dir/cloned-symlink"]
op = 'added'

[file."clone/into/existing/dir/cloned-symlink".diff]
mode = 'u+rwx,g+rwx,o+rwx'
file-type = 'symlink'
user = "root"
group = "root"
text = '/f'

[file."clone/into/existing/dir/deeper"]
op = 'added'

[file."clone/into/existing/dir/deeper".diff]
mode = 'u+rwx,g+rx,o+rx'
file-type = 'directory'
user = "antlir"
group = "antlir"
content_hash = "0"

[file."clone/into/existing/dir/deeper/nesting"]
op = 'added'

[file."clone/into/existing/dir/deeper/nesting".diff]
mode = 'u+rwx,g+rx,o+rx'
file-type = 'directory'
user = "antlir"
group = "antlir"
content_hash = "0"

[file."clone/into/existing/dir/deeper/nesting/foo"]
op = 'added'

[file."clone/into/existing/dir/deeper/nesting/foo".diff]
mode = 'u+r,g+r,o+r'
file-type = 'regular-file'
user = "root"
group = "root"
text = '''
This file will be cloned!
'''
