load("//antlir/bzl:build_defs.bzl", "rust_library")

oncall("twimage")

rust_library(
    name = "antlir2_features",
    srcs = glob(["src/**/*.rs"]),
    deps = [
        "memfd",
        "serde",
        "serde_json",
        "//antlir/buck/buck_label:buck_label",
    ],
)
