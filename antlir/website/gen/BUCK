load("//antlir/bzl:build_defs.bzl", "rust_binary")

oncall("twimage")

rust_binary(
    name = "generate-metalos-apis",
    srcs = ["generate_metalos_apis.rs"],
    unittests = False,
    deps = [
        "anyhow",
        "serde",
        "//antlir/buck/buck_query:buck_query",
    ],
)
