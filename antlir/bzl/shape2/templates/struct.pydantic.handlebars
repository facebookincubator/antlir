class {{name}}(Shape):
    __GENERATED_SHAPE__ = True

    {{#each fields}}
    {{#if required}}
    {{name}}: {{type-hint type}} {{#if (has-default-value this)}}= {{literal default_value type}}{{/if}}
    {{else}}
    {{name}}: typing.Optional[{{type-hint type}}] = None
    {{/if}}
    {{/each}}
