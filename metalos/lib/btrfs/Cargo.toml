# @generated by autocargo from //metalos/lib/btrfs:[btrfs,btrfs-container-unittest_test_binary]
[package]
name = "btrfs"
version = "0.0.0"
authors = ["Facebook"]
edition = "2021"
license = "MIT"

[lib]
test = false
doctest = false

[[test]]
name = "btrfs"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0.56"
async-compression = { version = "0.3.8", features = ["all-implementations", "brotli", "bzip2", "deflate", "gzip", "zlib", "zstd"] }
async-trait = "0.1.52"
bitflags = "1.3"
bytes = { version = "1.1", features = ["serde"] }
futures = { version = "0.3.13", features = ["async-await", "compat"] }
libc = "0.2.121"
metalos_paths = { path = "../metalos_paths" }
nix = "0.23"
tempfile = "3.2"
thiserror = "1.0.30"
tokio = { version = "1.15", features = ["full", "test-util", "tracing"] }
tokio-util = { version = "0.6", features = ["full"] }
uuid = { version = "0.8.1", features = ["serde", "v4", "v5"] }
zstd = "0.9.0+zstd.1.5.0"

[dev-dependencies]
slog = { version = "2.7", features = ["max_level_trace", "nested-values"] }
slog_glog_fmt = { path = "../../../common/rust/shed/slog_glog_fmt" }
systemd = { path = "../systemd" }

[features]
default = ["metalos_container_test"]
metalos_container_test = []
