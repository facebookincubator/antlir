# @generated by autocargo from //metalos/lib/image:[image,image-unittest]
[package]
name = "image"
version = "0.0.0"
authors = ["Facebook"]
edition = "2021"
license = "MIT"

[lib]
test = false
doctest = false

[[test]]
name = "image"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0.56"
async-trait = "0.1.52"
btrfs = { path = "../btrfs" }
bytes = { version = "1.1", features = ["serde"] }
derive_more = "0.99.3"
futures = { version = "0.3.13", features = ["async-await", "compat"] }
metalos_paths = { path = "../metalos_paths" }
reqwest = { version = "0.11.6", features = ["blocking", "json", "rustls-tls", "rustls-tls-native-roots", "stream", "trust-dns-optional"] }
slog = { version = "2.7", features = ["max_level_trace", "nested-values"] }
thiserror = "1.0.30"
tokio = { version = "1.15", features = ["full", "test-util", "tracing"] }
zstd = "0.9.0+zstd.1.5.0"

[dev-dependencies]
url = "2.2.2"

[features]
default = ["metalos_plain_test"]
metalos_plain_test = []
